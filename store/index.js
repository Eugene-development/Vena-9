export const state = () => ({
});

export const mutations = {
};

export const actions = {
};

export const getters = {
};

//Запретил строгий режим во VUEX. Сделал для работы с формами. Иначе ошибка.
// export const strict = false;
